let text = "Hell";
let min = 0;
let max = 1;

for (let i = 0; i < text.length; i++) {
    let result = text.substring(min, max);
    min++;
    max++;
    if ()
}
let result = text.substring(min, max);

console.log(text.length);